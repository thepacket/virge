# Copy to .env (gitignored) or export in your shell, then restart the dev server.
# The key is read by the Vite dev-server middleware only — it is never sent to
# the browser or committed.
ANTHROPIC_API_KEY=sk-ant-...
